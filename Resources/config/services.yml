services:
    app.locale_listener:
        class: OfCoding\UtilBundle\EventListener\LocaleListener
        arguments: [@router, "%kernel.default_locale%"]
        tags:
            - { name: kernel.event_listener, event: kernel.controller, method: onKernelController }
            - { name: kernel.event_subscriber }